import 'package:flutter/material.dart';
import './opt.dart';

void main() {
  runApp(
    MaterialApp(
      title: 'Flutter Tutorial',
      home: new Scaffold(
        body: new OptWidget(),
      ),
    ),
  );
}

// OptWidget manages the state.

